import { NavBar, MarketingFooter } from './../ui-components'

export default function Dressup() {
    return (
      <main style={{ padding: "1rem 0" }}>
        <NavBar width={"100vw"} />
        <h2>This is dressup page!</h2>
      </main>
    );
  }
